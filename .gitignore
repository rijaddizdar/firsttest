# Xcode / build artifacts
DerivedData/
build/
*.xcuserstate
xcuserdata/
.DS_Store

# The .xcodeproj is generated by XcodeGen (see project.yml) but is committed so
# the project can be opened without running xcodegen. Per-user state is ignored
# above; the shared scheme under xcshareddata is kept.
